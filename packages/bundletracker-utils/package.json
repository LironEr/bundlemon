{
  "name": "bundletracker-utils",
  "version": "0.1.0-rc.0",
  "main": "./lib/cjs/index.js",
  "module": "./lib/esm/index.js",
  "files": [
    "lib/"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/bundletracker/bundletracker.git",
    "directory": "packages/bundletracker-utils"
  },
  "author": "Liron Er",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build": "rimraf lib/ && tsc -p tsconfig.release.json && tsc -p tsconfig-cjs.json",
    "prepublishOnly": "yarn build && yarn test"
  },
  "dependencies": {},
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^3.9.3"
  }
}
