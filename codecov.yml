comment: false

flags:
  packages:
    paths:
      - packages/
  service:
    paths:
      - service/
coverage:
  status:
    patch: off
    project:
      default:
        only_pulls: true
        target: auto
